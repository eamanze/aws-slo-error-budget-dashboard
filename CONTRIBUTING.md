# Contributing

Create a focused branch, keep infrastructure changes reviewable, and run `make validate` and `make test` before opening a pull request. Changes to alerts must include an actionable runbook; changes to SLIs or SLOs must update `docs/slo.md` and explain the user impact.

